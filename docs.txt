study{
    js_es_6{
        debugger,
        Object.assign({}, obj, obj) === sread operator: ..arr, ..arr,
        arr.map,
        arr.filter,
        arr.reduce,
        return !condition ? true : false,
        rerutn !a ? a: b => a ?? b,
    }
    includes{
        import,
        extopt,
    }
    async,
    jQuery
    api{
        open weather api,
        the movie database .org docs,
        list view / grid view,
        https://developers.themoviedb.org/3/discover/movie-discover
        openweathermap.org/current
    }
    library{
        momentsjs: for Dates,
    }
}


update 03-03{
    
    транзишън на тъмба и показващия се текст,
    интегриран изкачащ прозорец в който да се зарежда динамичното видео,
    интегриран динамичен текст във долната част на попъп-а,
    интегрирано видео със нужните атрибути,
    добавена възможност за затваряне на отворения попъп,
}


очаквания за следващ спринт{
    добавяне на анимация докато сайтът не зареди напълно
    сортировка по има на лигата като ще има възможност да се избере една от топ 4 лигите или Шампионска Лига,
    добавяне на Френската Лига 1 към списъка на лиги с възможност за сортировка,
    добавяне на сортировка за търсене по име на отбор във всички лиги,
}
